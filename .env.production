# Force Next.js to install dependencies even if they're missing
NEXT_FORCE_INSTALL_DEPS=true

# Tell Next.js not to throw errors for missing dependencies
NEXT_IGNORE_MISSING_DEPENDENCIES=1

# Enable legacy peer dependencies to avoid errors
NODE_OPTIONS=--legacy-peer-deps
