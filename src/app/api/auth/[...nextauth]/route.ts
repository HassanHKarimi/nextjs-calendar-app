// This file sets up the NextAuth API route handler
// It imports and exports the GET and POST handlers from the auth configuration

import { GET, POST } from "@/auth";

// Export the route handlers for Next.js API routes
export { GET, POST };
